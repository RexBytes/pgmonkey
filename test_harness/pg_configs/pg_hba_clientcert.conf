# pg-clientcert: SSL required + client certificate verification
# Only SSL connections with a valid client cert (signed by our CA) are accepted.
# TYPE     DATABASE  USER  ADDRESS      METHOD
local      all       all                trust
hostssl    all       all   0.0.0.0/0    scram-sha-256  clientcert=verify-ca
hostssl    all       all   ::/0         scram-sha-256  clientcert=verify-ca
